# 当前进度记录

## 总体阶段判断

- 当前状态：双关可展示原型
- 所处阶段：已完成最小可玩闭环，正在向双关垂直切片早期推进
- 当前完成度判断：已经适合对外展示双关样片，但还没有进入正式内容生产阶段

## 已完成阶段

### 阶段 1：最小可玩闭环

- 主菜单、开始游戏、战斗、结果页完整打通
- 玩家移动、自动射击、受伤、死亡和一局结束流程成立
- 火力升级、炸弹、Boss、HUD 和结果页基础形态完成

### 阶段 2：展示原型增强

- 敌群前中后段节奏重排
- 火力成长提升到更清楚的 Lv5 链路
- 炸弹资源提示和清屏表现增强
- Boss 拥有更强的分段压迫与阶段切换
- 增加暂停、重开、返回菜单等外层流程

### 阶段 3：展示可读性与关底收束强化

- 结果页重组为更像展示版的总结界面，包含战绩卡片、三段摘要和下一步建议
- Boss 击破后增加战场清场总结卡，让战斗收束与结果页之间有明确过渡
- Boss 入场、相位切换、核心暴露和 overdrive 全部接入事件卡提示
- 关键补给、压位段与夹击段也接入事件卡提示，整局节奏提示更加连续
- 高速侧入和双侧夹击波次增加边缘入口预警，进一步强化战场阅读性

### 阶段 4：视听反馈与 Boss 演出强化

- 增加独立 Boss 入场锁定特效和 Boss 击破冲击特效
- 强化命中、爆炸与炸弹波纹层次，提升击中和清屏的体感
- 增加程序生成的占位音乐，并区分战斗、Boss、overdrive、失败和通关收束
- 为 Boss 入场、Boss 击破和失败收束增加电影边栏包装
- 增加低血量与关底高压阶段的持续危险压屏，补足战场氛围层

### 阶段 5：敌机职责扩展与第二关骨架验证

- 增加新的 `screener` 敌机职责，用慢速大颗粒弹形成可读的屏障火力路线
- 主菜单增加 `Stage 01` 与 `Stage 02` 选择入口
- 战斗主控改为通过关卡目录切换数据脚本，不再把单关写死在主流程里
- 新增 `Stage 02 // Storm Front` 关卡骨架，用于验证第二关的节奏、Boss 参数和扩展成本
- 结果页和全局状态加入关卡标识，保证多关卡原型下的结算信息仍然清楚

### 阶段 6：第二关打磨与双关章节串联验证

- `Stage 02` 增加更明确的中前段补给窗口、后段狙击叠压和完整事件提示
- `Stage 02` Boss 改成独立的 `storm` 风格，拥有不同于第一关 Boss 的专属火力节奏与外观
- 主菜单新增 `Chapter Run`，可以直接从 `Stage 01` 接入 `Stage 02`
- 章节模式下会继承生命、炸弹和火力，并在关卡交接时给出明确的阶段过渡提示
- 结果页现在能承接下一关交接与章节总结两种结果，不再只适用于单关结算

### 阶段 7：第二关展示完成度提升

- 为第二关补了新的 `suppressor` 压制扇面敌机职责，用宽扇面火力更明确地逼迫玩家提前站位
- 第二关波次中加入 `suppressor` 与 `pincer`、`anchor`、`screen` 的组合，后段节奏比骨架版更完整
- 章节模式进入第二关时增加了更明确的 `LEG 2` 过渡提示和镜头包装
- 结果页增强了章节交接面板与章节总评面板，让 `Chapter Run` 的结束更像完整样片

### 阶段 8：章节收束增强与第二关敌群深化

- 第二关继续强化了 `suppressor` 波次，把压制扇面正式纳入中后段路线决策
- 章节最终收束增加了 `CHAPTER SECURED` 与 `CHAPTER COMPLETE` 级别的战斗内提示
- 结果页现在会显示章节等级、分关等级和更明确的章节总评
- 双关打完后，结果页的 `Retry` 已调整为真正的 `Retry Chapter`

### 阶段 9：章节演出增强与风暴互动

- 第二关新增 `storm_strike` 风暴打击互动点，用预警线和落雷束逼迫玩家重排路线
- 第二关背景层加入更明确的 `storm` 主题视觉，章节第二段的气质和第一关进一步拉开
- 结果页新增章节路线条、下一段简报和章节尾声文案，章节交接与章节结束更像完整演出
- 单独进入第二关时，现在会使用展示用的中段装载，而不是从 `Lv1 / 2 bomb` 硬起手

### 阶段 10：章节过场场景落地

- 新增独立的 `ChapterBriefing` 场景，作为 `Stage 01` 与 `Stage 02` 之间的真正过场
- `Chapter Run` 流程从“结果页直接跳下一关”升级成“结果页 -> Briefing -> 下一关`
- 过场会展示下一关标题、继承装载、章节时间线和下一段 mission brief
- 自动验证已覆盖 `Stage 01 -> Results -> Briefing -> Stage 02` 的完整双关链路

### 阶段 11：章节镜头化过场与尾声分层

- `ChapterBriefing` 从信息页升级成带电影边栏、扫描线和多区块任务面板的章节过场
- 双关章节通关后新增独立 `ChapterOutro` 场景，负责章节尾声、章节总评和下一步 directive
- 章节模式流程升级成 `Stage 01 -> Results -> ChapterBriefing -> Stage 02 -> Results -> ChapterOutro`
- 自动验证已覆盖完整双关章节链路，确认新增场景不会打断 `--autoplay --chapter`

### 阶段 12：第二关环境联动增强

- 第二关新增独立 `storm_sweep` 环境机关，用横向扫掠补强风暴区间的空间压迫
- 新增 `storm_cross` 事件，让纵向 `storm_strike` 与横向 `storm_sweep` 形成十字封线联动
- `ChapterBriefing` 和 `ChapterOutro` 补入章节卡片，让双关路线在交接和尾声阶段更清楚
- 自动验证已覆盖 `storm_cross` 出现后的 `Stage 02` 单关与 `Chapter Run` 完整链路

### 阶段 13：第二关联动编排与 ending 收束增强

- `storm_cross` 现在支持 `support_wave`，环境机关和支援敌群可以在同一个节奏点里联动压路线
- 第二关两段 `storm_cross` 已带上 suppressor 和 sniper 支援，后段不再只是机关单独压迫
- `ChapterOutro` 新增短暂的 ending 封板，让双关通关后的收束更像章节 ending 而不是直接进总结面板
- 自动验证已覆盖联动编排增强后的 `Stage 02` 和完整双关章节链路

### 阶段 14：双关 ending 分层

- 新增独立 `ChapterEnding` 场景，专门承接双关通关后的章节结尾镜头
- 双关章节流程升级成 `Stage 01 -> Results -> ChapterBriefing -> Stage 02 -> Results -> ChapterEnding -> ChapterOutro`
- 结果页在章节完成后不再直接进入 debrief，而是先进入 ending，再落到完整章节复盘
- 自动验证已覆盖新的完整双关链路，确认 `ChapterEnding` 不会打断 `--autoplay --chapter`

### 阶段 15：第二关 Boss 与环境联动

- 第二关 Boss 配置新增 `phase_2_hazard`、`phase_3_hazard` 和 `overdrive_hazard`
- Boss 相位切换和 overdrive 现在会主动带出风暴机关，关底段落不再只靠 Boss 本体压场
- Boss 风暴机关仍沿用现有 `storm_strike / storm_cross` 管线，保持了环境模块的可复用性
- 自动验证已覆盖 Boss 级联动增强后的 `Stage 02` 与完整双关章节链路

### 阶段 16：垂直切片收口判断

- `ChapterEnding` 增加了更明确的 route seal 封板，章节 ending 更像独立结尾镜头
- 主菜单文案调整为更明确的“双关垂直切片候选”表达，方便当前 build 做展示和评审
- 新增 `docs/vertical-slice-review.md`，明确当前 build 的阶段判断、亮点、短板和后续两条路径
- 自动验证已覆盖单关与双关链路，确认收口阶段的展示层调整没有破坏主流程

### 阶段 17：路线 A 收口打磨

- `ChapterEnding` 继续补强为带 route seal、路线收束带和章节结尾提示音的章节 ending 镜头
- 主菜单和 README 现在更明确地表达“当前正在按路线 A 朝双关垂直切片收口”
- 自动验证已覆盖单关、第二关和完整双关链路，确认这一轮视听包装增强没有破坏流程

### 阶段 18：终盘节拍精修

- 第二关 Boss 终盘新增“最后安全窗口”提示，明确 overdrive 前的资源决策点
- `ChapterEnding` 增加了 `SLICE VERDICT` 结论卡，让路线 A 的阶段判断在画面里也能直接表达
- 自动验证已覆盖这一轮节拍提示与 ending 精修后的单关、第二关和完整双关链路

## 当前大阶段结论

- 当前版本已经从单关展示样片进入双关可展示原型，章节串联已经成立
- 当前结构已经不只是能承载多关卡，还能承载中场过场、章节交接和章节总评
- 下一步更值得做的是继续强化章节镜头表现和环境互动，而不是继续堆基础系统

## 当前版本亮点

- 单关和双关两种展示路径都成立
- 第二关已经形成自己的敌群语言，不再只是第一关的数值加压版本
- 风暴打击让第二关开始有自己的环境互动逻辑
- `Chapter Run` 已经有比较明确的开始、交接、终结三段包装
- 结果页不仅给出成绩，还承担了章节总评与复盘职责
- 中场 briefing 和章节 outro 已经从结果页中拆出来，章节流分层更清楚
- 第二关开始具备“敌群压迫 + 环境十字封线”这类更接近正式关卡的联动压力
- 第二关已经开始出现“机关先锁路线，敌群再补刀”的正式关卡节奏
- 双关章节的结尾现在已经分成“ending 镜头”和“debrief 面板”两层
- 第二关 Boss 现在也开始和环境机关联动，关底高潮更接近完整关卡设计
- 当前 build 已经非常接近一个可以明确宣称的“双关垂直切片候选”
- 当前工作已经明显进入“打磨和收口”阶段，而不是继续横向扩系统
- 路线 A 现在已经进入最后一轮节拍和包装精修区间

## 当前仍缺少的内容

- 正式音效和真正可长期使用的背景音乐资源
- 更强的命中停顿、击破停顿和视听同步细节
- 更强的镜头驱动过场和更有仪式感的章节尾声演出
- 第二关节拍虽然已可展示，但还没有像第一关那样被细抠到最佳状态
- 更长链路的章节串联、章节间成长或资源设计
- 第二关环境机关虽然已成立，但还缺更多能与敌群协同的复合设计
- 章节 ending 已经独立成 scene，但还可以继续抬升镜头表现
- 现在已经接近一个很明确的“双关垂直切片判断点”
- 如果继续推进，后续重点应转向“精修和包装”，而不是继续横向堆系统
- 路线 A 已经启动，后续更适合做最后一轮精修和封版判断
- 继续推进的话，更合理的是准备封版评审，而不是继续引入新玩法层

## 下一阶段建议

### 优先级最高

- 继续补真正可记忆的章节镜头节点，而不只是静态 UI 包装
- 继续给 `Stage 02` 补更复杂的环境联动和敌群协同
- 把当前 `ChapterEnding` 继续推进成更完整的最终 ending scene

### 阶段 19：路线 A 封版前精修

- 第二关 Boss 终盘补成了 `LAST SAFE WINDOW -> OVERDRIVE -> FINISH WINDOW` 三段决策节拍，最后破口比之前更明确
- `ChapterEnding` 新增章节评审就绪卡，把 FLOW、PRESSURE、REVIEW 三个维度直接展示出来
- 当前路线 A 已经更接近“封版前最后一轮精修”，后续更适合做最终收口而不是继续扩系统

### 阶段 20：路线 A 最终封版阶段补强

- 第二关 Boss 的 `FINISH WINDOW` 现在会带出独立 `FINAL BREACH` 终盘编排，最终破口和中心突破线更明确
- `ChapterEnding` 新增 `FINAL PASS` 面板，把当前 build 是否已经足够作为 review build 展示直接说清楚

### 阶段 21：最终封版总结与包装可见化

- 主菜单新增 build 状态摘要，当前 build 的定位不再只写在文档里
- `ChapterOutro` 新增 `FINAL PACKAGE` 与 `NEXT STEP` 面板，把封版结论直接放进章节 debrief
- 新增 `docs/final-slice-summary.md`，作为当前双关垂直切片候选的正式封版总结

### 阶段 22：最终项目总总结

- 新增 `docs/final-project-report.md`，把项目定位、当前完成度、亮点、缺口和后续建议统一收口
- 当前文档层已经具备：开发说明、进度记录、垂直切片评估、封版总结、演示清单和最终项目总总结

### 阶段 23：当前封版候选标记

- 主菜单把 `Chapter Run` 明确标成当前推荐展示入口
- 新增 `docs/release-candidate-note.md`，正式说明当前版本已经可以作为封版候选使用

### 阶段 24：最终交付说明

- 新增 `docs/final-delivery-note.md`，把当前版本作为阶段性交付版本的用法和展示方式写清楚
- 主菜单把 `Chapter Run` 进一步明确成推荐演示入口

### 阶段 25：当前封版交接说明

- 新增 `docs/final-handoff-note.md`，把这版作为当前封版候选的交接方式、演示路径和后续建议写清楚

### 阶段 26：最终封版清单与人工演示说明

- 新增 `docs/final-freeze-checklist.md`，把当前版本该如何冻结和交付列成清单
- 新增 `docs/manual-demo-guide.md`，把人工演示的推荐顺序和强调点整理出来

### 阶段 27：当前封版公告

- 新增 `docs/final-release-announcement.md`，把当前版本正式标记为当前封版候选
- 当前版本已经更接近“可封版的双关垂直切片候选”，后续重点更应该转向最终总结和封版判断
- 然后正式决定是否进入双关垂直切片阶段

### 阶段 28：菜单可用性与语言设置补强

- 新增持久化的中英文语言设置，当前会保存在 `user://settings.cfg`
- 主菜单增加 `中文 / English` 切换入口，默认中文，切换后会立即刷新当前场景
- 主菜单内容列重新按可视宽度居中计算，修正启动时文本区域过窄、视觉偏移和标题截断问题
- 主菜单里暴露的 build 状态、演示路线、演示检查表和最近战绩摘要现在会跟随语言切换

### 阶段 29：战斗文本本地化与火力成长重调

- 战斗 HUD、结果页、章节简报、章节结尾和章节总结里的主要用户可见文本已接入中英切换，不再只停留在主菜单层
- 第一关与第二关的事件提示、Boss 段说明和关卡元数据补入中文文本源，减少过场页和结算页的英文残留
- 第二关章节结尾与章节总结页面改成更适合 540 宽视口的窄屏排版，避免多卡片横排时被右侧裁切
- 普通敌机的随机火力掉落概率整体下调，并把保底阈值后移，成长节奏不再过早拉满
- 玩家 `Lv4 -> Lv5` 火力差距被拉开：高等级主炮密度更高、僚机数量更多、射击节拍更快，满火力的体感明显增强

### 阶段 30：主菜单与结算页窄屏可用性修正

- 主菜单改成可滚动的窄屏自适应布局，不再依赖过大的最小内容宽度
- 关卡入口按钮在 540 宽窗口下会改为单列展示，避免文字从左右两侧挤出屏幕
- 结果页改成“主体内容可滚动 + 底部按钮固定显示”的结构，避免按钮被长文本挤到屏幕外

### 阶段 31：章节页面窄屏适配与 Boss 节奏回调

- `ChapterBriefing`、`ChapterEnding`、`ChapterOutro` 进一步统一成“主体内容可滚动 + 底部操作区固定”的窄屏结构，避免 540 宽窗口下正文或按钮被挤出可视区
- `ChapterOutro` 的 ending overlay 不再写死超宽最小尺寸，章节结尾镜头在窄屏下也能完整显示标题和摘要
- 战斗 HUD 顶部信息区整体压缩并降低遮挡感，Boss 条、横幅和事件卡的高度与位置都做了上移和收窄
- Boss 新增进场锁血窗口，同时下调炸弹对 Boss 的直接伤害并上调双关 Boss 生命值，避免“刚出场就被秒”的失真节奏
- 自动验证已再次覆盖单关与完整 `--autoplay --chapter` 链路，确认这一轮页面与 Boss 调整没有破坏章节闭环

### 阶段 32：章节页交互修正与战斗音效柔化

- 结果页、章节简报、章节结尾和章节总结统一补上鼠标滚轮滚动支持，正文区域不再依赖必须对准滚动条才能上下翻
- `ChapterOutro` 的 ending overlay 在动画结束后会主动释放，避免残留全屏控件继续压住滚轮和底部按钮点击
- 章节结尾页的 route seal 中央封板动画结束后会隐藏，进一步降低滚动时的输入干扰
- 结果页章节总结区改成竖向文本列，章节总结与尾声文案不再重叠
- 高频战斗音效继续往“经典街机 STG 的低中频短击感”收拢：射击、命中、击破与 Boss 受击整体降亮、降响并拉开层次，连续射击时不再那么刺耳

### 阶段 33：外层控件表现与资源节奏收紧

- 主菜单、结果页、章节简报、章节结尾、章节总结和暂停菜单统一接入按钮视觉样式，主操作和次操作的层级更明确，hover / pressed / disabled 状态更容易一眼看懂
- 火力资源继续从“随机一路喂满”往“关键补给窗口”靠拢：单关与双关流程的随机掉落保底都后移了一档，成长更依赖中段明确给出的补给点
- 射击、小敌受击和击破音效继续下压高频与响度，战斗连续开火时更顺耳，不会一直顶在最前面抢注意力

### 阶段 34：战斗手感再强化

- 拾取加入更明显的上收线自动吸附和中距离磁吸，资源回收比之前顺手，展示玩法时不容易因为补给擦肩而过打断节奏
- HUD 新增炸弹资源动态提醒，`BOMB WINDOW OPEN / NO BOMB BUFFER` 不再只是静态文字，而会直接带动炸弹区做脉冲强调
- 普通大敌与 Boss 核心暴露阶段的命中、击破反馈再次分层，重目标现在会带出更明确的轻量 hitstop、震屏和屏幕脉冲

### 阶段 35：采样音效管线与混音分层落地

- 新增 `AudioBusSetup` 全局初始化器，把战斗音频正式拆成 `BGM`、`SFX_Player`、`SFX_Enemy`、`SFX_Boss`、`SFX_UI` 五类总线，不再把所有声音都直接堆在 `Master`
- `SfxController` 改成“采样优先、程序音回退”的结构：核心事件会优先播放本地 wav 采样，缺失时才退回程序合成，后续替换正式素材时不需要再改调用层
- 新增 `assets/audio/sfx` 占位采样包和 `tools/generate_audio_samples.ps1` 生成脚本，当前提供 `player_shot`、`enemy_hit`、`enemy_destroy`、`player_hurt`、`boss_break` 等核心事件的多变体采样，占位包已具备后续直接替换为正式录音素材的结构
- `BgmController` 也接入 `BGM` 总线，音乐与高频战斗音效终于有了基本的层级分离
- 击毁、受伤、受击和玩家射击重新拉开了音色职责：击毁恢复更厚的爆裂感，玩家受伤不再和敌人爆炸抢同一块听感位置，连续开火时整体也更稳

### 阶段 36：外部免费素材接入与混音回调

- `SfxController` 现在支持 `.ogg + .wav` 混合加载，Kenney 与 Shapeforms 的原始素材可以直接接入，不需要先全部转码
- 已用 Kenney 的 `Digital Audio`、`Interface Sounds` 替换玩家射击、拾取、升级和通关提示等高频轻音效
- 已用 Shapeforms 的 `Sci Fi Weapons`、`Future UI`、`Hit and Punch`、`Sci Fi Spacecraft` 替换敌人受击、敌人击毁、玩家受伤、Boss 命中、Boss 预警和 Boss 击破等关键战斗事件
- 音频总线和事件音量又做了一轮回调：玩家射击、玩家受伤和 UI 提示整体下压，敌人击毁和 Boss 事件保留更强存在感，连续战斗时不再互相抢最前排
- 新增 `docs/audio-sources.md`，记录当前已接入素材来源、授权背景和未接入的 Sonniss 阻塞点

### 阶段 37：Sonniss 重型补位与音量设置收口

- Sonniss 的重型素材已经接入，主要负责 `boss_warning`、`boss_break`、`bomb` 和第二关 `storm_charge / storm_impact` 这类更需要厚重动态的事件
- `game_stage2.gd` 现在会在 `storm_strike / storm_cross` 的预警与落点阶段触发专属风暴音效，第二关环境机关终于不再只靠画面和文字提示
- `SfxController` 增加了按事件裁尾的播放上限，长素材不会把整段尾巴完整铺出来，Boss 击破、炸弹和风暴冲击现在都更利落
- `RunState` 增加了可持久化的 `BGM / SFX` 音量设置，`AudioBusSetup` 会按用户设置实时回调各总线电平
- 主菜单现在有完整的音频设置区，暂停面板里也补了快速音量滑杆，试玩时不需要再靠改脚本调整听感
- 风暴冲击素材又做了一轮资源侧优化：`storm_impact_01` 已从 70MB 级别的大雷声替换成更短、更利落的版本，仓库同步和后续迭代都更稳
- 新一轮混音把 `player_hurt / enemy_destroy / boss_warning / storm_impact` 这几类关键事件重新拉开层级，并在 SFX 总线上补了压缩，重点解决“击毁太弱、受伤太抢、整体忽大忽小”的问题
- 这一轮继续把常听到的采样按变体补偿做了“源文件级”的归一化处理，同时收紧 Lv4 之后的随机火力掉落，并把 HUD 顶部状态条、Boss 条和事件卡再压瘦一档
- 本地工具里补了 `run_local_debug.cmd` 和 `clear_godot_processes.cmd`，方便在试玩前隔离 `.godot-user` 并清掉卡死的 Godot 进程
- 又补了一轮高频事件替换：`player_shot / power_up / bomb_pickup` 已从旧的轻量占位音切到 Shapeforms 科幻采样，同时 `wav` 现在会优先于旧 `ogg` 被加载
- 战场逻辑区域现在整体下移到 HUD 下方，敌机、子弹、拾取和机关不再直接顶到顶部文字区
- 顶部 HUD 继续收口：事件卡现在会优先占用固定顶部条带，普通横幅让位，不再和事件卡叠成两层文字
- `boss_warning / boss_phase / storm_charge / storm_impact / bomb` 又做了一轮压级和裁尾，临近 Boss 的提示声不再像之前那样过于冲前
- 当前音效试验链路已经切成 `wav` 素材优先且仅加载 `wav`，旧 `ogg` 资源即使还留在目录里，也不会再参与播放
- 顶部现在补了固定深色遮罩带，HUD 信息明确占上方固定区，战场从它下方开始，不再是简单把整块游戏画面往下挪
- 战场坐标重新回到“本地可玩区 + 顶部固定 HUD”的结构：`world_layer` 现在有独立的基础偏移，震屏只在这个基础上叠加，不会再把 HUD 预留区和战场边界一起拖乱
- 顶部 HUD 再次收紧并完全压进固定条带，顶部遮罩改成不透明，Boss 条、横幅和事件卡都限制在同一段高度内，战场可视起点终于和逻辑起点重新对齐
- `boss_warning / boss_phase / storm_charge / storm_impact / bomb` 继续按素材和事件双层压级，现阶段测试链路已经明显偏向“素材本声”为主，不再让几类大提示音抢掉整个战场
- 顶部主 HUD 重新排版成“左侧章节/分数/进度，右侧双列资源状态”的结构，动态提示回到左侧，右边不再堆成一长列；顶部背景也从厚重黑边改成更轻的半透明层
- 顶部 HUD 又做了一轮观感收口：背景黑边继续减重，Boss 条/横幅/事件卡整体上提并收窄，右侧资源文字缩短，Boss 段顶部压迫感还在，但不再显得头重脚轻
- 顶部 HUD 继续轻量化：主面板、Boss 条和事件卡透明度进一步下调，顶部背景高度继续回收，顶部信息现在更像“贴在画面上的轻信息层”，而不是压住画面的厚 UI 盖板
- 顶部 HUD 字体和警戒层又做了一轮分级：Boss 条颜色与字号从普通 HUD 里拉开，横幅与事件卡字号继续回收，顶部现在更接近“常驻信息 / Boss 警戒 / 短时提示”三层分工
- 顶部 HUD 最后一轮质感统一继续往 Boss 段倾斜：Boss 条低血量时会主动抬高警戒色，常态与危险态的视觉差更明确，顶部终于不再只是“缩小后的信息堆叠”，而更像有主次的街机警戒层
- 进入垂直切片收口阶段后，HUD 的资源显示逻辑已经清掉重复覆盖，暂停面板、清场提示和结果页详情文本也统一回当前双语方案
- 展示导向的平衡继续微调：高火力与章节继承状态下的火力掉落进一步收紧，炸弹窗口提示前移，第一关与第二关 Boss 的血量和攻击频率都略有上调
- 新增 `docs/stable-showcase-release.md`，把当前双关切片的完整体验、验证范围、控制说明、已知限制和下一阶段建议收成了一份稳定展示版说明
- 人工演示说明、最终演示检查清单和封版候选说明都已重写成可直接交付的中文文档，之前的乱码版本不再适合作为展示材料
- 新增 `docs/showcase-qa-report.md`，把本轮 headless 启动、单关自动流程和双关自动流程的结果集中记录下来，方便后续试玩和封版对照
- 工具目录补充了 `tools/run_showcase_verification.cmd`，现在可以一键完成“启动检查 + 单关验证 + 双关验证”的展示版自检流程
- 展示版自检在发布仓库里额外抓到了第一关旧脚本 `game.gd` 的拾取信号兼容问题，现已补齐为和 `pickup.gd` 一致的参数签名
- 当前工作区和 `.publish-final` 的主流程文件已经重新同步，发布仓库现在也能稳定跑出完整的双关 `CHAPTER_RESULT`
- 进入试玩反馈打磨阶段后，高火力掉落又收紧了一轮：高火力、后半段和章节继承状态下更不容易轻松喂满 `Lv5`
- `Lv5` 本体也被重新拉开辨识度：主炮伤害、支援机伤害和射速都略有提升，让后段成长更像真正奖励，而不是和 `Lv4` 几乎同感
- 第一关与第二关 Boss 的生命、射速和后段警戒节奏再次上调，炸弹窗口与“最后安全窗口 / 终结窗口”也会更早给出提示，整体更贴近展示版所需的街机压迫感
- 展示候选版本标识已统一为 `RC-0.4`，主菜单、README 和稳定展示版说明现在都围绕这个候选版本进行表达
- 新增 `docs/release-package-checklist.md` 与 `tools/prepare_showcase_package.cmd`，后续可以按固定清单准备对外试玩包

### 阶段 38：公开 Demo 准备开局

- 当前推进方向从“继续扩展示原型”切换为“公开 Demo 准备”，`RC-0.4` 保留为稳定展示基线
- 新增 `docs/public-demo-roadmap.md`，明确 Demo 目标、非目标、推荐里程碑和验收标准
- 新增 `docs/commercial-gap-analysis.md`，把距离商业成品的主要差距拆成内容量、美术、音频、系统、关卡、技术发布和市场包装
- 新增 `docs/external-playtest-plan.md`，用于指导 5 到 10 人小范围外部试玩和反馈记录
- 新增 `tools/run_demo_readiness_check.cmd`，把展示版自动验证和展示包生成串成一次 Demo 就绪检查
- README 已更新为公开 Demo 准备阶段入口，后续推进会优先围绕试玩、打包、授权和首屏体验，而不是继续默认横向堆系统

### 阶段 39：Demo 1 首次体验打磨

- 主菜单首屏从“开发状态看板”调整为“试玩入口优先”：标题、推荐完整 Demo、首次试玩提示会先出现
- 黄色主按钮改为 `开始完整 Demo / 推荐入口：章节连打`，让外部试玩者不必理解内部关卡结构也能直接开始
- 单关入口降级为 `练习入口（可选）`，仍保留 Stage 01 / Stage 02 便于调试和补练
- 首屏新增首次试玩提示，明确自动射击、炸弹按键、重开和返回主菜单
- 主菜单下方的版本状态和 Demo 判断文案改成更短、更面向试玩反馈，不再把开发总结堆在首屏前段

### 阶段 40：Demo 2 视听第一印象启动

- 主菜单新增动态扫描/跑道背景层，首屏不再只是静态色块，更接近可公开试玩的街机展示入口
- 玩家机占位图形增强了外轮廓、驾驶舱、火力光晕和 Lv5 双侧前向光束，火力成长在视觉上更容易感知
- 爆炸特效增加烟尘外圈、旋转火花和更多碎片线，击破反馈比旧版更厚
- 高频音效和 BGM 做轻量降疲劳回调：玩家射击、拾取、敌机击毁、Boss 预警和程序 BGM 都稍微后退，避免长时间试玩时抢注意力
- 主菜单 `_t()` 文案调用补回完整中英参数，并新增主菜单 headless 启动检查作为本轮验证重点
- 打包脚本改为每次生成带随机后缀的展示包，避免 Windows 旧 `dist` 权限残留导致 Demo 就绪检查失败

### 阶段 41：Demo 3 外部试玩回收准备

- 新增 `docs/playtest-quick-start.md`，作为发给试玩者的简短玩法说明和操作说明
- 新增 `docs/playtest-feedback-form.md`，用于记录单名玩家的行为、评分和开放反馈
- 新增 `docs/playtest-session-notes.md`，用于观察者按场次记录共性问题和下一版优先级
- 新增 `docs/playtest-decision-matrix.md`，把试玩反馈转成 P0 / P1 / P2 / 暂不处理的开发判断
- 新增 `tools/prepare_playtest_packet.cmd`，会先跑 Demo 就绪检查，再把最新 Demo zip 与试玩文档整理到一个 `dist/playtest-packet-rc-0.4-*` 资料夹
- `README` 和 `docs/external-playtest-plan.md` 已同步外部试玩资料夹流程，下一步可以开始组织 5 到 10 人小范围试玩

### 阶段 42：Demo 4 公开包准备

- 新增 `docs/public-demo-release-note.md`，作为公开 Demo 候选包的对外说明
- 新增 `docs/public-demo-known-issues.md`，明确当前限制、技术提示和暂不处理项
- 新增 `docs/capture-checklist.md`，整理 3 到 5 张截图和 30 到 60 秒短视频的采集建议
- 新增 `docs/asset-license-checklist.md`，把 Kenney、Shapeforms、Sonniss 和程序化资源的授权复核事项集中记录
- 新增 `docs/public-demo-package-checklist.md`，定义公开 Demo 包应包含和不应包含的内容
- 新增 `tools/prepare_public_demo_packet.cmd`，会先生成试玩资料夹，再整理公开 Demo 候选资料夹

### 进入垂直切片前需要确认

- 是继续把现有双关做成更完整的可展示垂直切片
- 还是在确认节奏后继续扩出第三段或更多章节骨架
