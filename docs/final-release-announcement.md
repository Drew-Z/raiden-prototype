# 当前封版公告

## 公告

当前版本正式标记为本项目的当前封版候选版本。

## 当前版本包含

- 可完整跑通的 `Stage 01`
- 可完整跑通的 `Stage 02`
- 可完整跑通的 `Chapter Run`
- 完整的 `ChapterBriefing -> ChapterEnding -> ChapterOutro` 外层流程
- 可用于评审、演示、交接的配套文档

## 推荐用途

- 内部评审
- 对外演示
- 阶段性交付
- 下一阶段开发的基线版本

## 默认约定

如果没有新的明确开发目标，当前版本默认视为冻结中的封版候选，不继续默认扩展系统范围。
